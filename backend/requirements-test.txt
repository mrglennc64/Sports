# Test dependencies for smoke test suite
# Run: pip install -r requirements-test.txt

# Core testing framework
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-timeout>=2.1.0
pytest-cov>=5.0.0

# API testing
httpx>=0.28.0
respx>=0.22.0

# Fixtures and factories
pytest-factoryboy>=2.7.0

# Performance monitoring
pytest-benchmark>=4.0.0

# Output formatting
pytest-html>=4.1.0
pytest-json-report>=1.5.0

# Data validation
pydantic>=2.10.0

# Async support is provided by pytest-asyncio (above) + the stdlib asyncio module.
# (Do NOT add a PyPI `asyncio`/`unittest-mock` backport — those are stdlib on 3.x
# and the ancient backports break installs on modern Python.)

# Environment management
python-dotenv>=1.0.0

# Code coverage
coverage>=7.0.0
