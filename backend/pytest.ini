[pytest]
testpaths = tests
pythonpath = .
# Crypto-vertical tests use `async def` with @pytest.mark.asyncio; auto mode lets
# pytest-asyncio run them without per-test event-loop boilerplate.
asyncio_mode = auto
